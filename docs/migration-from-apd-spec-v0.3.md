# Migration From APD-Spec AER v0.3

This repository starts a clean AER schema at `version: "1.0"`.

The older AER v0.3 shape from APD-Spec is not consumed or emitted by this toolkit.
Future migration tooling can map v0.3 records into this schema, but Phase 1 focuses on
Claude Code JSONL as the reference input.
